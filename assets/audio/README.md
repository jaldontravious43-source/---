# 音频资源说明（BGM + 音效）

请将音频文件放在当前目录，文件名如下（当前版本只使用音效，不使用 BGM）：

- `hook-launch.mp3`：发钩音效
- `score-pop.mp3`：加分提示音
- `win.mp3`：达标结算音
- `lose.mp3`：未达标结算音

说明：

- 当前按上面的 `mp3` 文件名直接读取。
- 如果某个文件缺失，游戏会打印 warning，但不会崩溃。
- 浏览器策略要求先点一次“开始游戏”，音频才能解锁播放。
